name: Generate Renders

on:
  push:
    branches: [ main ]

jobs:
  example:
    runs-on: ubuntu-latest
    container: setsoft/kicad_auto:9.0
    steps:
    - uses: actions/checkout@v2
    - uses: INTI-CMNB/KiBot@v2_k8
      with:
        config: config.kibot.yaml
    - name: upload results
      if: ${{ always() }}
      uses: actions/upload-artifact@v4
      with:
        name: output
        path: output